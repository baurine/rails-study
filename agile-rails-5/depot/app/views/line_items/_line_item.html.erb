<tr>
  <td><%= line_item.product.title %></td>
  <td><%= line_item.quantity %>&times;</td>
  <td><%= number_to_currency(line_item.product.price) %></td>
  <td class="item_line"><%= number_to_currency(line_item.total_price) %></td>
</tr>
